# Introduction: a school calculator that takes a list of marks you got in a list of subjects and calculates the total of it. and gets percentage out of 100.

name_subjects = int(input("Enter the number of subjects you wanna calculate marks for: "))

marks = []
for i in range(name_subjects):
    mark = float(input(f"Enter marks for subject {i+1}: "))
    marks.append(mark)

total_marks = sum(marks)
percentage = (total_marks / (name_subjects * 100)) * 100

print(f"Total marks: {total_marks}")
print(f"Percentage: {percentage:.2f}%")

